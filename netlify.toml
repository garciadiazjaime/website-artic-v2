[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[edge_functions]]
  path = "/*"
  function = "entry.netlify-edge"

[context.production.environment]
  QUIZ_URL = "${QUIZ_URL}"